pub use async_trait::async_trait;
pub use bytes::Bytes;
pub use chrono::{DateTime, Utc};

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
